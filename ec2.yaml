- name: create ec2 and r53
  hosts: local
  connection: local
  tasks:
    - name: create ec2 instance
      amazon.aws.ec2_instance:
        name: "{{ item }}"
        image_id: "ami-09c813fb71547fc4f"
        instance_type: "t3.micro"
        security_group: ["sg-01d559ae05d4950f8"]
        loop:
        - mongod